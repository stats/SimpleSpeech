export type CommunicationButton = {
  id: string;
  label: string;
  phrase: string;
  imageDataUrl?: string;
  placeholder?: string;
  sortOrder: number;
  createdAt: string;
  updatedAt: string;
};
